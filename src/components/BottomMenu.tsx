import React from "react";
import styles from './style.module.scss';

const BottomMenu: React.FC = () => {
    return (
        <div className={styles.menuContainer}>BottomMenu</div>
    )
}

export default BottomMenu;